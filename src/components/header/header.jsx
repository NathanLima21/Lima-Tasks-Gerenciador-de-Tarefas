import React from 'react';
import './header.css';

const Header = () => {
  return (
    <header className="app-header">
      <h1 className="app-title">Lima Tasks</h1>
      <p className="app-subtitle">Seu gerenciador de tarefas inteligente</p>
    </header>
  );
};

export default Header;